{
    "buildCommand": "cd reme ; npm install ; npm run build",
    "framework": "vite",
    "outputDirectory": "reme/dist",
    "rewrites": [
        {
            "source": "/server/(.*)",
            "destination": "/server/server.js"
        }
    ]
} 